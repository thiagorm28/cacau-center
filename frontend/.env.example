# Copie para `.env` neste diretório (`frontend/.env`, gitignorado) e ajuste se necessário.

# URL base da API do backend. Sem essa variável o cliente HTTP monta URLs relativas
# à própria origem do Vite (localhost:5174), o que resulta em 404 em toda chamada.
VITE_API_URL=http://localhost:3001
